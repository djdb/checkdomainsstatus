checkdomainsstatus

Проверяет статус списка доменов (наличие A-записи, MX-записи, web-страницы). Скрипт поможет определить количество "живых" доменов из большого списка. Поддерживается многопоточность.

Для работы скрипту нужен файл domains.txt, лежащий в одном каталоге со скриптом. Формат файла с данными: один домен на одну строчку.
